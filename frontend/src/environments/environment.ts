// Ambiente de produção. O build de produção (`ng build`) usa este arquivo
// (veja o "fileReplacements" da configuration "production" em angular.json).
export const environment = {
  production: true,
  apiUrl: 'http://localhost:8080'
};
