/**
 * Espelha o enum `RoleUsuario` do backend (com.climaservice.api.entity.RoleUsuario).
 * Não inventar valores aqui: qualquer novo perfil só existe depois de existir no backend.
 */
export type Role = 'ADMIN' | 'ATENDENTE' | 'TECNICO';
